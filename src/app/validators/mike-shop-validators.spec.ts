import { MikeShopValidators } from './mike-shop-validators';

describe('MikeShopValidators', () => {
  it('should create an instance', () => {
    expect(new MikeShopValidators()).toBeTruthy();
  });
});
